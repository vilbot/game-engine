/build/
/.cache/
/compile_commands.json

# runtime artifacts (DEBUG file I/O test, hot reload, replay), can land at
# repo root or in build/ depending on the cwd the executable was launched from
/test_copy.cpp
/replay.hmi
libgame_temp_*.dylib
libgame_temp_*.so
libgame_temp_*.dll
*.dSYM/
*.pdb
*.ilk

# Dear ImGui runtime layout (window positions, docking) - regenerated on exit
/imgui.ini
